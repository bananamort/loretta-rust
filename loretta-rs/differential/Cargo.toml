[package]
name = "differential"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Rust side of the Loretta-RS differential oracle — writes the same JSON as tools/differential (C# reference) for corpus inputs per LuaVersion preset"

[dependencies]
loretta = { path = "../loretta" }
full_moon.workspace = true
